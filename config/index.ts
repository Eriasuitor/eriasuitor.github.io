import { ConfigType } from './config';

const config: ConfigType  = {
  name: '赶紧拿小本本记到',
  media: [{domain: 'https://github.com/eriasuitor', icon: '/github.png', name: 'Eriasuitor'}]
};

export default config;